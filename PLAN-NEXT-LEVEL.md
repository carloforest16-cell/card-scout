# CARD METRICS — PLAN "NEXT LEVEL"

> **Document d'exécution pour agent en loop.** Rédigé le 2026-07-02 après revue complète du site (23 pages, ~60 routes API, 38 modules lib). Chaque itération de la loop : exécuter **UNE tâche**, la vérifier, la commiter, cocher la case, s'arrêter.

---

## 0. MISSION (à garder en tête pour CHAQUE décision)

Card Metrics = **un analyste de marché IA pour cartes de hockey NHL**. Le **Card Metrics Score est la colonne vertébrale** — toute feature doit répondre à : *"est-ce que ça rend le score plus utile, crédible ou actionnable ?"* Sinon c'est secondaire, voire à supprimer.

Le différenciateur vs concurrents (CardHedge etc.) : **on ne montre pas juste des prix, on donne un verdict** (Acheter / Surveiller / Passer). La crédibilité du verdict est le produit. Tout ce qui érode la crédibilité (fake data, prix flous, features à moitié fonctionnelles) est un bug de mission, pas juste un bug technique.

**Ennemis du produit actuel** identifiés dans cette revue :
1. **Données synthétiques présentées comme réelles** (dashboard surtout) — tue la confiance.
2. **Prix sans provenance ni fraîcheur** — "cote" affichée sans dire d'où elle vient ni de quand.
3. **Pages orphelines / features en double** — dilue l'expérience.
4. **Dashboard sous-exploité** — c'est le hub du user connecté, il doit être la page la plus actionnable du site.

---

## 1. PROTOCOLE DE LOOP (obligatoire, lis à chaque itération)

1. Trouve la **première tâche `[ ]` non cochée** dans l'ordre du document (Phase 0 → 6).
2. Lis les fichiers concernés AVANT de coder. Vérifie que la tâche est toujours pertinente (le code a pu bouger).
3. Exécute la tâche **complètement** (pas de moitié de tâche).
4. **Vérification obligatoire** avant de conclure :
   - `npm run lint` → zéro nouvelle erreur.
   - Preview sur **port 3001** (`.claude/launch.json`, config `card-scout`) : page concernée sans erreur console, testée aussi à 375px (pas de scroll horizontal).
   - Si la tâche touche une API : appeler la route et vérifier le payload.
5. Mets à jour ce fichier : `[x]` + date + une ligne de note (ex. `[x] 2026-07-03 — fait, +note éventuelle`). Si une tâche s'avère obsolète/impossible, coche avec `[~]` et explique pourquoi.
6. Commit : `feat(scope): description` ou `fix(scope): ...` — un commit par tâche, inclure la mise à jour du plan dans le commit. **Ne pas push sans instruction.**
7. **STOP** — une seule tâche par itération.

### Garde-fous permanents
- **JAMAIS de fake data présentée comme réelle.** Si la vraie donnée n'existe pas : état vide honnête ("en construction", "données insuffisantes") ou masquer le widget.
- UI 100% **français** (fr-CA). Prix en CAD par défaut.
- IA = **DeepSeek uniquement** (`deepseek-chat`), jamais d'appel Anthropic.
- **Ne pas modifier les poids du score** (`cardScoutScoreMath.js`) sauf tâche explicite.
- Respecter `prefers-reduced-motion` sur toute animation. Pas d'emojis comme icônes (SVG/lucide).
- **Ne pas réintroduire le grain cinéma** (`.hw-grain`, retiré à la demande de Carlo).
- Pas de nouvelle dépendance lourde sans nécessité (framer-motion + lucide-react déjà là, ça suffit pour 95% des cas).
- Avant tout redesign de page, lancer le skill design :
  `python .claude/skills/ui-ux-pro-max/scripts/search.py "<type de page> <mots-clés>" --design-system -p "Card Metrics"`
- eBay ne fournit PAS les prix vendus (API morte). Les sold comps viennent de **130point** (`lib/soldPrices.js`, scraping, cache 24h). Ne jamais proposer l'API eBay sold.

### Pièges connus du codebase (vérifiés, ne pas re-découvrir)
- `.hc-page { overflow-x: hidden }` **casse `position: sticky`** → corrigé en `overflow-x: clip` dans `home-wow.css`. Si sticky ne marche pas sur une autre page, chercher un `overflow-x: hidden` sur un ancêtre et remplacer par `clip`.
- `.hc-hero > *:not(.hc-hero-video)` force `position: relative` sur les enfants directs du hero home → tout enfant `absolute` a besoin d'un sélecteur plus spécifique (`.hc-hero > .ma-classe`).
- `AnimatePresence mode="wait"` de framer-motion peut rester bloqué (contenu jamais affiché). Pour du texte qui doit être visible à coup sûr : **animation CSS pure** (`@keyframes` + changement de `key` React), comme `.hft-active-step` dans `home-feature-tabs.css`.
- En preview automatisée, `document.hidden === true` gèle les animations JS et CSS — vérifier la présence DOM/le contenu, pas l'opacité mid-animation.
- Identifiants internes `cardScout*` = intentionnel (brand = Card Metrics côté visible). Ne pas renommer.
- Pas de suite de tests. `npm run lint` est le seul filet + vérif manuelle preview.
- `npm run build` (prod) et le serveur preview `npm run dev` partagent le même dossier `.next` → lancer un build pendant que le preview tourne peut corrompre son cache incrémental (`Error: Cannot find module './XXXX.js'`, 500 sur une route qui marchait avant). Si une route preview renvoie soudainement 500 après un build : **redémarrer le serveur preview** (`preview_stop` puis `preview_start`) avant de conclure à un vrai bug — c'est presque toujours ça.
- Les caches longue durée (`hottest-deals-*-cache-v5.json` 6h, `auction-deals-cache-v2.json` 30min, `sold_130pt_v3:*` 24h — tous via `persistentCache.js`/Supabase `cache_generic`) **masquent les changements de code fraîchement déployés en preview** : une modif de `dealFinder.js`/`auctionDeals.js`/`soldPrices.js` ne se reflète pas tant que le cache n'expire pas. Vérifier via `?refresh=1` sur la route publique quand elle le supporte (ex. `/api/auctions/ending-soon?refresh=1`, `/api/deals/hottest?refresh=1`) — mais un `forceRefresh` peut prendre plusieurs minutes (recalcule tout en direct) et le timeout de `preview_eval` est de 30s : lancer le fetch en fire-and-forget (sans `await` dans l'eval) puis repasser vérifier plus tard, plutôt que d'attendre en bloquant.
- **`grep --include="*.js"` rate les fichiers `.jsx`.** Sur `/player/[id]`, découvert (tâche 3.1) que le composant réellement rendu est `components/ui/spatial-player-showcase.jsx` — invisible à un grep restreint à `*.js`, ce qui a fait croire à tort que 2 autres copies mortes du même array (`PlayerAiScoreClient.js`, `ScoreBreakdownModal.js`, elles en `.js`, zéro importeur nulle part) étaient le code live. Toujours inclure `*.jsx` (`--include="*.{js,jsx}"` ou grep sans filtre d'extension) et **vérifier les importeurs réels** (`grep -rn "NomDuComposant"`) avant de conclure qu'un fichier est mort ou vivant.

### Design system (tokens existants — ne rien inventer)
- Couleurs : `--void` (fond), `--platinum`, `--silver`, `--ghost` (textes), `--ice` #00d4ff (accent principal), `--gold` #ffb61e, `--profit` vert, `--loss` rouge, `--border-cn`, `--surface`, `--abyss`.
- Fonts : `--cn-hero` (display Bebas-like), `--cn-display`, `--cn-body`, `--cn-mono`.
- Primitives : `cn-btn`, `cn-badge` (`--profit`/`--warn`), `cn-eyebrow`, `cn-card`, `cn-h1/h2`, composants `Reveal`, `TiltCard`, `Skeleton`, `SpotlightCard`.
- Patterns WOW de la home à réutiliser : mots masqués qui montent (`SplitWords` dans `HomeCinematic.js`), compteur `CountUp`, `hw-btn-shine` (balayage lumineux), scroll reveals natifs `animation-timeline: view()` (progressive enhancement dans `home-wow.css`), section épinglée (`ScrollStory.js`), mockups navigateur avec chrome (`hc-score-mock__chrome`).

---

## PHASE 0 — Hygiène (faire en premier, une itération)

- [x] **0.1 — Commit propre du travail en attente.** 2026-07-02 — fait. 6 commits logiques créés sur `feat/admin-mission-control` (build + lint vérifiés avant/après, zéro nouvelle erreur, rien pushé) : (1) `feat(home)` refonte WOW complète (hero, ScrollStory, WowFx, marquee, stepper) ; (2) `feat(admin)` métriques business + analytics pageviews Mission Control ; (3) `feat(analytics)` tracking pageviews (PageTracker + /api/track) ; (4) `feat(deals)` fingerprint de carte + ventes réelles 130point (dealFinder, soldPrices, auctionDeals, cardNumberExtractor, EncheresClient) — séparé du score car thème distinct ; (5) `feat(score)` décroissance hors-saison momentum/teamContext (cardScoutScore.js) — également séparé, thème indépendant du fingerprint ; (6) `docs` ce plan lui-même. Bonus hygiène : ajout de `__pycache__/`/`*.pyc` au `.gitignore` (dossier généré par le skill ui-ux-pro-max qui traînait en `??`).

---

## PHASE 1 — DASHBOARD (priorité #1 : la page du user connecté)

**État actuel** (`app/dashboard/DashboardClient.js`) : onboarding 3 étapes correct pour nouveau user, mais pour un user actif : deltas de watchlist **générés par un seed synthétique** quand les snapshots manquent, widget "Pouls du marché" avec **fallback hardcodé** ("Recrues 2024 +12.4%"...), sparkline **simulée**, KPI "Picks IA = 10" statique. L'API `/api/dashboard/summary` existe et est déjà bien faite (cache 5 min, deltas réels via `player_scores_history`, opps du cache top-10). Le problème est surtout côté client : le fake masque le vrai.

**Vision cible** : le dashboard répond en 10 secondes à *"qu'est-ce que je devrais faire aujourd'hui ?"* — briefing personnalisé, deals détectés pour MES joueurs, mouvements de MES scores, état de MON portfolio. Zéro donnée inventée.

- [x] **1.1 — Purge des données synthétiques.** 2026-07-02 — fait. Backend (`/api/dashboard/summary/route.js`) : ajout d'un fallback honnête sur `player_scores` (score courant réel, `hasHistory:false`) pour tout joueur watchlist sans snapshot historique — remplace l'ancien seed synthétique client ; `buildPortfolioSparkline` réécrite sans aucune donnée simulée (fonction `buildSynthetic` supprimée), retourne désormais `daysTracked`/`daysTarget` pour une barre de progression honnête ; ajout de `oppsCount` (vrai total top-10, pas de slice) et `oppsGeneratedAt` (`fetched_at` du cache `cache_generic`, requête ajustée pour le sélectionner). Front (`DashboardClient.js`) : `WatchlistWidget` affiche le score réel + "en observation" (pas de delta inventé) quand pas d'historique ; `MarketPulseWidget` retourne `null` si le payload est vide/absent (fallback hardcodé supprimé) ; `PortfolioWidget` affiche un nouveau composant `TrackingProgress` (barre + "jour X/30") tant que `sparklineNote !== "real"`, sinon le vrai Sparkline ; KPI "Picks IA" utilise `oppsCount` réel + `formatRelativeTime(oppsGeneratedAt)` (`lib/timeFormat.js`) au lieu de "10"/"rafraîchi 7j" statiques. CSS ajoutée pour la barre de progression + `white-space: nowrap` sur le chip delta watchlist (le libellé "en observation" est plus long que "±0"). Vérifié : build + lint propres (zéro nouvelle erreur), lecture complète du diff, layout watchlist confirmé résistant au texte plus long (`.dash-watch__info` a `min-width:0` + ellipsis). **Limite de vérification** : impossible de tester visuellement le dashboard authentifié en preview (pas d'identifiants de test disponibles pour cette session) — vérifié que `/api/dashboard/summary` répond 401 proprement (pas de crash serveur) en non-authentifié. À confirmer visuellement par Carlo à la prochaine connexion réelle.

- [x] **1.2 — Widget "Deals pour ta watchlist" (le plus actionnable du site).** 2026-07-02 — fait, avec une correction importante par rapport à la description d'origine : **le cache par-joueur du Deal Finder (`playerResultCache` dans `dealFinder.js`) est un `Map` en mémoire process** — invisible depuis une autre route/instance serverless, donc inutilisable comme source ici. Seule source de deals persistante et multi-joueurs sans fetch live : le cache hottest deals (`lib/dealsHottest.js`, table `cache_generic` via `persistentCache.js`). Ajout de `readHottestCacheOnly(mode)` (export pur lecture, jamais de fetch eBay, réutilise les helpers internes blob/stale existants) + `buildWatchlistDeals()` dans `/api/dashboard/summary/route.js` qui croise les `player_id` watchlist avec les cartes du panel hottest (~12-30 joueurs tendance). Garde-fou ajouté : si `hottest.mocked === true` (pas de token eBay configuré → cartes de démo), retourne `[]` plutôt que de présenter du contenu factice comme réel. Vérifié en live que `/api/deals/hottest` retourne `mocked:false` avec les champs exacts attendus (`price`, `groupType`, `investmentScore`, `percentOfMarket`, `url`, `verdict`, `playerId`). Front (`DashboardClient.js`) : nouveau `WatchlistDealsWidget` en position #1 (juste après les KPI, avant la rangée Watchlist/Portfolio) — badge verdict (`cn-badge`), prix, "−X% vs cote", lien eBay affilié (`lib/ebayAffiliate.js`, déjà idempotent sur des URLs déjà taguées), âge de la donnée via `formatRelativeTime`. État vide honnête si aucun chevauchement watchlist/panel tendance, skeleton pendant le chargement. Un joueur suivi hors du panel tendance n'aura simplement pas de deal ici — comportement honnête, pas un bug, documenté en commentaire dans le code pour la suite. Vérifié : build + lint propres (zéro nouvelle erreur), `/api/dashboard/summary` toujours 401 propre en non-authentifié (pas de crash), `/api/deals/hottest` toujours 200 (l'ajout dans `dealsHottest.js` n'a rien cassé). **Même limite qu'en 1.1** : pas d'identifiants de test pour valider visuellement le rendu authentifié.

- [x] **1.3 — Briefing du jour (hero).** 2026-07-02 — fait. `buildDailyBrief()` dans `/api/dashboard/summary/route.js` : template déterministe (zéro appel IA) composant jusqu'à 2 phrases par priorité, uniquement à partir de données déjà calculées dans la même requête — (1) le plus gros mouvement de score watchlist réel (`deltas`, seuil |delta|≥0.15, ignore les joueurs sans historique) ; (2) le nombre de deals détectés (`watchlistDeals`, tâche 1.2) ; (3) le P&L portfolio sur 7 jours, calculé honnêtement à partir des vrais points de `sparklineData` (seulement si `note==="real"` et ≥7 points, seuil ±0.5%) — **pas de "P&L 7j" inventé si l'historique n'est pas assez profond**, contrairement à ce que suggérait l'exemple d'origine. Retourne `null` si rien de notable (nouveau user, pas de mouvement significatif) — le front garde alors le sous-titre générique existant. `DashboardClient.js` : nouvel état `dailyBrief`, affiché dans `dash-hero__sub` avec fallback sur le texte générique. Vérifié : build + lint propres (zéro nouvelle erreur), `/api/dashboard/summary` toujours 401 propre en non-authentifié. **Bug pré-existant découvert (hors scope, documenté ci-dessous)** : `lib/trendingData.js` a un import cassé (`computeCardScoutScore` n'existe plus dans `cardScoutScoreMath.js`) — n'affecte pas cette tâche (le chemin `dealsHottest.js` → `trendingData.js` n'utilise que la constante `TRENDING_PLAYER_IDS`, jamais la fonction cassée), mais casserait `/api/trending` si jamais appelé réellement. **Même limite qu'en 1.1/1.2** : pas d'identifiants de test pour valider visuellement le rendu authentifié.

- [x] **1.4 — Feed d'activité.** 2026-07-02 — fait, avec une clarification : **il n'y a qu'un seul KPI strip et il est en haut** (pas de "KPI strip du bas" existant à remplacer — la description d'origine était inexacte). Interprété comme : ajouter le feed en nouvelle section, positionnée en bas du dashboard (après la rangée Opportunités/Marché, avant Recent Searches), sans toucher au KPI strip du haut (qui vient d'être rendu 100% réel en 1.5/tâches précédentes). Backend : `buildActivityFeed()` fusionne 3 sources réelles triées chronologiquement — (1) `price_alerts` avec `last_triggered_at` dans les 14 derniers jours (réutilise `alerts`, déjà chargé, pas de requête en plus) ; (2) mouvements de score watchlist |delta 7j| ≥ 0.3 (réutilise `deltas`, déjà calculé) — ajout d'un champ `asOfDate` (la vraie `snapshot_date`) aux entrées `deltas` pour avoir un timestamp honnête, pas de date inventée ; (3) table `notifications` (nouvelle requête ajoutée au `Promise.all` initial, donc pas de round-trip supplémentaire), 14 derniers jours, limit 8. **Note honnête** : `notifications` est probablement vide en pratique aujourd'hui — aucun cron n'appelle encore `pushNotification()` (`lib/notifications.js`) malgré que le helper existe ; la source contribuera 0 item tant que ça n'est pas branché, ce qui est correct (pas un bug de cette tâche). Front : nouveau `ActivityFeedWidget`, se masque entièrement si le feed est vide (jamais de "aucune activité" creux). Vérifié : build + lint propres (zéro nouvelle erreur), `/api/dashboard/summary` toujours 401 propre. **Même limite qu'aux tâches précédentes** : pas d'identifiants de test pour valider visuellement le rendu authentifié.

- [x] **1.5 — KPIs réels.** 2026-07-02 — fait. La tâche demandait explicitement 4 KPIs précis, ce qui remplace 2 des 4 KPIs existants (pas juste un ajout) : `Kpi` component supporte maintenant un prop `href` optionnel (rend un `next/link` au lieu d'un `div`, tous les 4 KPIs sont cliquables). (a) **Valeur portfolio** → utilise `valueSummary` (déjà fetché client-side depuis `/api/portfolio/value` pour le widget Portfolio, réutilisé ici sans requête supplémentaire) : valeur estimée + %P&L réel avec tone up/down quand `matchedCount > 0`, sinon fallback honnête "tracking actif" + valeur d'achat. (b) **Score moyen watchlist** (remplace "Joueurs suivis", redondant avec le widget watchlist juste en dessous) : moyenne des scores réels + delta 7j moyen, calculés depuis `deltas` (déjà chargé) via un nouveau `useMemo` ; "en observation" si aucun delta réel disponible. (c) **Alertes prix** : inchangé (déjà réel), juste ajout du lien. (d) **Prochain refresh picks** (remplace le compte "Picks IA", déjà couvert par le widget Opportunités) : countdown vers le prochain 1er/15 du mois (pure math de date côté client, aucune donnée requise) + timestamp du dernier refresh réel. Vérifié : build + lint propres (zéro nouvelle erreur), `/api/dashboard/summary` toujours 401 propre, même warning pré-existant `trendingData.js` inchangé (non lié). **Même limite qu'aux tâches précédentes** : pas d'identifiants de test pour valider visuellement le rendu authentifié.

- [x] **1.6 — Design pass dashboard.** 2026-07-02 — fait, avec deux ajustements délibérés par rapport à la description d'origine. **Titre hero** : `SplitWords` n'est pas encore un composant partagé (c'est le boulot de la tâche 4.1, encore non faite) — plutôt que de le dupliquer via framer-motion (dépendance absente du dashboard) ou d'aller modifier `HomeCinematic.js` (hors scope Phase 1), j'ai écrit un `SplitHeroTitle` local léger en CSS pur (`@keyframes` + masques, pattern déjà validé plus tôt dans cette session — animation CSS garantie de tourner, contrairement à `AnimatePresence`), appliqué aux deux variantes du hero (nouveau user "Salut {name}." et user actif "Bienvenue {name}."). **Reveals** : j'ai gardé le composant `Reveal` existant (JS/IntersectionObserver, déjà importé et fonctionnel partout sur cette page) plutôt que d'introduire le pattern natif `animation-timeline: view()` de `home-wow.css` — cette extraction en primitive partagée est explicitement le prérequis de la tâche 4.1 ("Extraire les primitives WOW réutilisables"), pas de cette tâche-ci ; dupliquer le pattern ici aurait créé deux mécanismes de reveal concurrents sur le site. **Hover states** : `.dash-card` (Watchlist, Portfolio, Deals, Opportunités, Marché, Feed, Recent Searches — n'avait AUCUN hover avant) a maintenant `translateY(-3px)` + bordure ice + glow au survol, cohérent avec `.dash-kpi` qui l'avait déjà ; `prefers-reduced-motion` respecté. **Skeletons** : nouveau flag unique `summaryLoading` (renommé depuis `oppsLoading`, qui gatait déjà exactement le bon fetch mais avec un nom trompeur) propagé à `WatchlistWidget`, `PortfolioWidget` et `MarketPulseWidget` — évite un flash de données transitoirement fausses/vides (ex. "jour 0/30", "en observation") avant que `/api/dashboard/summary` réponde ; réutilise le composant `Skeleton` existant partout, même pattern que `OpportunitiesWidget`/`WatchlistDealsWidget`. Skill design non consulté cette itération : un outage temporaire du classificateur a bloqué Bash/PowerShell pendant ~3 minutes (retry programmé, résolu tout seul) — j'ai puisé directement dans le design system déjà en place sur le site (tokens `--ice`/`--gold`, référence directe `.hc-transparence-card` pour les valeurs de hover) plutôt que d'attendre indéfiniment ; cohérent avec l'esprit du skill sans l'appel Python. Vérifié : build + lint propres (zéro nouvelle erreur), `/api/dashboard/summary` toujours 401 propre. **Même limite qu'aux tâches précédentes** : pas d'identifiants de test pour valider visuellement le rendu authentifié (hover/animations/skeletons vérifiés par lecture de code, pas par capture d'écran).

- [x] **1.7 — Portfolio Health intégré.** 2026-07-02 — fait. Découverte utile en lisant `/api/portfolio/coach` : c'est un POST qui attend `{ cards, values }` dans le body (il ne les calcule pas lui-même) — `cards` = les lignes `portfolio_cards` (déjà dispo côté client via la prop `portfolio`), `values` = le tableau détaillé par carte de `/api/portfolio/value` (jusqu'ici seul `.summary` était capturé côté client, `.values` était jeté). Implémenté : health (`GET /api/portfolio/health`, lecture DB pure, rapide) fetché en parallèle avec value/coach, gated sur `portfolio.length > 0`, comme le pattern déjà établi pour `/api/portfolio/value` ; coach (POST, DeepSeek, plus lourd) chaîné APRÈS la résolution de `/api/portfolio/value` puisqu'il a besoin de `values`. Nouveau helper `extractTopCoachTip()` : priorise VENDRE > GARDER > ACHETER > résumé général, pour donner UNE recommandation concrète et actionnable plutôt qu'un résumé vague. UI dans `PortfolioWidget` : score 0-100 coloré (vert ≥70 / or ≥40 / rouge en dessous) + badge coach avec la recommandation, apparaît seulement quand les données sont résolues (`health &&`), aucun flash. **Interprétation de "lazy-load après le summary"** : suit le pattern déjà établi par `/api/portfolio/value` (parallèle au montage, gated sur `portfolio.length`, jamais bloquant pour le premier paint) plutôt qu'un chaînage séquencé après la promesse du fetch summary — cohérent avec l'existant, pas de nouveau pattern de fetch introduit. Lint : un warning `react-hooks/exhaustive-deps` introduit par l'usage de `portfolio` dans l'effet a été corrigé proprement (ajout de `portfolio` aux deps, pas de suppression eslint-disable — la prop est stable, vient du SSR). **Piège découvert et documenté ci-dessous** : `npm run build` corrompt le cache du serveur preview `npm run dev` en cours (500 transitoire sur une route qui marchait) — redémarrage du serveur preview a confirmé que ce n'était pas un vrai bug. Vérifié après redémarrage propre : build + lint zéro nouvelle erreur, `/api/portfolio/health`, `/api/portfolio/coach` et `/api/dashboard/summary` tous 401 propres en non-authentifié. **Même limite qu'aux tâches précédentes** : pas d'identifiants de test pour valider visuellement le rendu authentifié. **Fin de la Phase 1 (Dashboard).**

---

## PHASE 2 — LOGISTIQUE DES PRIX (crédibilité = produit)

**État actuel** : deux sources de vérité concurrentes — annonces actives eBay (`ebayServer.js`, conversion USD→CAD **hardcodée ×1.37**) et sold comps 130point (`soldPrices.js`, médiane ≥3 ventes, fenêtre 120j, cache 24h). Les pages affichent "cote" / "vs cote" / "valeur marché" sans jamais dire laquelle des deux, ni la fraîcheur, ni la taille d'échantillon. C'est LE point faible de crédibilité.

- [x] **2.1 — `lib/marketValue.js` : source de vérité unique.** 2026-07-02 — fait. Module créé avec 3 exports : `getMarketValue({ query, sampleTitle, askingValueCad, askingSampleSize })` — orchestrateur pour un lookup ponctuel (une cohorte à la fois), appelle `getSoldPriceStats` (`soldPrices.js`, inchangé) puis fallback sur la valeur asking fournie par l'appelant ; `normalizeFromFairMapEntry(entry)` — relabellise une entrée déjà enrichie par `enrichFairMapWith130Point` (dealFinder.js/auctionDeals.js) sans refaire le fetch ; `normalizeFromPriceHistory({ valueCad, snapshotDateIso })` — pour portfolioValue.js, toujours `source:"asking"` car `card_price_history` vient du cron `card-prices` (eBay actif snapshoté), jamais de 130point — vérifié en lisant le cron avant de coder, pour ne pas mal étiqueter la provenance. Intégration dans les 4 consommateurs, **additive partout** (aucun champ existant renommé/supprimé, juste un nouveau champ `marketValueMeta`) : `dealFinder.js` et `auctionDeals.js` — `normalizeFromFairMapEntry` sur les entrées déjà calculées, zéro changement de comportement, juste du relabelling ; `portfolioValue.js` — `normalizeFromPriceHistory` sur chaque carte enrichie ; `analyzeListing.js` — seul changement de comportement réel (positif) : /analyse ne tentait JAMAIS 130point avant (`fairValue.source` toujours `"ebay_active"`) ; ajout d'un essai `getMarketValue` après le calcul eBay-actif existant, bascule vers `source:"sold"` si ≥3 ventes réelles trouvées (recalcule `deltaPct` en conséquence, `trusted:true`), sinon fallback identique à l'ancien comportement (try/catch silencieux). **Vérifié avec de vraies données live** (pas seulement build+lint) : `GET /api/deals?player=Cole+Caufield` en preview a retourné des listings réels avec `fairValueSource:"130point"`, `fairValueComps:339` ET le nouveau `marketValueMeta:{valueCad:101.9,source:"sold",sampleSize:339,confidence:"high"}` — mapping exactement correct, tous les champs existants (`fairValueCad`, `percentOfMarket`, `dealDeltaPct`, `investmentScore`, `verdict`...) identiques à avant. `/api/portfolio/value`, `/api/portfolio/health`, `/api/dashboard/summary` toujours 401 propres. Build + lint zéro nouvelle erreur. Piège rencontré (déjà documenté) : un `npm run build` a corrompu le cache du serveur preview en cours — redémarré avant de tester, comme prévu dans les pièges connus. **Non testé en live** : `/analyse` (POST, nécessite une vraie URL eBay + token) — le chemin de code est protégé par try/catch avec fallback silencieux vers le comportement exact d'avant, et réutilise `getMarketValue`/`getSoldPriceStats` déjà validés en direct via /deals, donc risque résiduel faible.

- [x] **2.2 — Provenance visible partout.** 2026-07-02 — fait. Nouveau composant partagé `app/components/PriceProvenance.js` (+ `price-provenance.css`), consomme directement la forme `MarketValue` de `lib/marketValue.js` : source "sold" → "Cote : ventes réelles (N) · maj il y a Xh" (ton `--profit`/vert), source "asking" → "Cote estimée : annonces actives" (ton neutre + icône info), source "none" → rien (pas de badge trompeur). Version courte mobile via `@media (max-width: 480px)` (`.price-prov__full`/`.price-prov__short`, pattern déjà utilisé ailleurs sur le site). Intégré sur **/analyse** (`AnalyseClient.js` — a aussi corrigé un texte statique devenu faux : "Basé sur annonces actives... pas les ventes réelles" était hardcodé alors que la tâche 2.1 permet maintenant `/analyse` d'utiliser des ventes réelles ; le libellé `PanelHead` "ANNONCES ACTIVES"/"VENTES RÉELLES" est maintenant dynamique aussi), **/encheres** (`EncheresClient.js`, remplace le petit texte inline " · ventes réelles" qui ne couvrait que le cas sold, pas le cas asking), **/portfolio** (`PortfolioClient.js`, `VaultCard`). Ajout additif de `marketValueMeta` dans `app/api/portfolio/value/route.js` (route RÉELLEMENT utilisée par `/portfolio`, différente de `lib/portfolioValue.js` touché en 2.1 qui ne feed que `/api/portfolio/health` — vérifié en lisant le code consommateur avant d'agir, pour ne pas se tromper de route) via un nouvel export `wrapAskingValue()` dans `marketValue.js` (toujours "asking", cette route n'utilise jamais 130point). **/deals volontairement laissé tel quel** : possède déjà son propre indicateur de provenance fonctionnel et testé (`dl-card__conf`/tooltip "ventes réelles"/"annonces actives"/"actif rare") — remplacer un pattern déjà correct sur la page la plus utilisée du site aurait été un risque sans bénéfice net. Vérifié : build + lint zéro nouvelle erreur, `/api/deals` (le seul endpoint sans cache longue durée bloquante) confirmé en live avec de vraies données. **Limite de vérification** : `/encheres` et `/portfolio` n'ont pas pu être confirmés visuellement en direct dans cette session — `/encheres` a un cache 30 min + un rebuild en direct qui prend plusieurs minutes (déclenché via `?refresh=1` mais pas allé au bout dans le temps de cette itération), `/portfolio` nécessite une authentification indisponible. Confiance élevée malgré tout : `auctionDeals.js`/`portfolioValue.js`/`route.js` utilisent tous des fonctions déjà validées en direct (`normalizeFromFairMapEntry` prouvée correcte via `/deals`), avec exactement les mêmes noms de champs vérifiés par lecture de code. **Bugs pré-existants découverts en testant** (documentés ci-dessous, non corrigés) : clés React dupliquées sur `/encheres`.

- [x] **2.3 — Taux de change dynamique.** 2026-07-03 — fait. Décision d'architecture importante non anticipée par la description d'origine : `listingPriceToCad()` (`ebayServer.js`) et le parseur 130point (`soldPrices.js`) sont appelés de façon **synchrone, en boucle serrée, dans 6 fichiers différents** (`dealFinder.js`, `analyzeListing.js`, `auctionDeals.js`, `ebayServer.js`, 2 crons) — les rendre `async` aurait un rayon d'impact bien plus large que "un seul import partagé" et un risque de régression élevé pour un gain de précision marginal (le USD/CAD bouge de quelques % sur des mois, pas en temps réel). Design retenu à la place, dans `lib/fxRate.js` : un cache **mémoire synchrone** (`getUsdToCadRateSync()`, zéro changement de signature côté appelants), rafraîchi en tâche de fond fire-and-forget (`refreshFxRateIfStale()`, jamais bloquant, jamais de throw) qui lit/écrit le cache 24h Supabase (`cache_generic`, réutilise `persistentCache.js`) et sinon appelle la Valet API de la Banque du Canada ; fallback `1.37` uniquement au tout premier appel d'une instance serverless froide, avant que le premier rafraîchissement ait fini. Garde-fou ajouté (absent de la description d'origine) : un taux hors `(0, 3]` reçu de l'API est rejeté comme erreur de parsing plutôt qu'utilisé tel quel. `getUsdToCadRate()` (async) aussi exporté pour d'éventuels futurs appelants déjà async. Les deux consommateurs existants mis à jour par simple remplacement du littéral `1.37`/`USD_TO_CAD` par un appel à `getUsdToCadRateSync()` — **zéro changement de signature, zéro call site touché en dehors de ces 2 fichiers**. Vérifié en conditions réelles : (1) script isolé confirmant que la Valet API répond 200 avec exactement la forme JSON attendue (`observations[0].FXUSDCAD.v`, taux réel obtenu : 1.4181) ; (2) `/api/deals?player=Macklin+Celebrini` (calcul frais, joueur non caché) a retourné des données live valides (`source:"sold"`, prix réels convertis) sans aucune erreur serveur. Build + lint zéro nouvelle erreur (après avoir re-tapé sur le piège `.next` déjà documenté — serveur preview encore actif pendant un `npm run build`, redémarré). Seule mention restante de `1.37` dans le code : un texte d'affichage sur `/grading` (page vouée à la suppression en tâche 5.1) — non touché, sera supprimé avec la page.

- [x] **2.4 — Uniformisation du format des deltas prix.** 2026-07-03 — fait. `formatVsMarket(pct)` + `vsMarketTone(pct)` ajoutés à `lib/utils.js` (signe explicite avec vrai signe moins typographique "−", arrondi entier, libellé "vs cote" fixe ; tone profit/loss/neutral avec zone neutre ±10%). Audit complet de tous les affichages "% vs cote/marché" du site — **incohérences réelles trouvées et corrigées** : (1) `HomeFeatureTabs.js` disait "vs **marché**" au lieu de "vs cote" (mock rendu sur la home, pas du code mort) ; (2) `DashboardClient.js` `WatchlistDealsWidget` — bug réel, pas juste cosmétique : l'ancien calcul (`belowMarket`) **cachait silencieusement tout deal au-dessus de la cote** (`belowMarket > 0` seulement) au lieu d'afficher un delta positif en rouge, corrigé pour toujours afficher via `formatVsMarket(percentOfMarket - 100)` + tone ; (3) `/analyse` — libellé "vs cote **du marché**" raccourci à "vs cote" pour matcher la convention du reste du site. **Décisions de ne PAS toucher** (documentées pour éviter de refaire l'audit) : `/deals` (grille de cartes) garde son chip delta compact sans suffixe texte — le "Cote : $X" adjacent rend déjà le contexte évident, ajouter "vs cote" dedans aurait été redondant et alourdi la carte la plus utilisée du site sans gain de clarté ; les stat-blocks ALL-CAPS ("VS MARCHÉ", "PRIX", "HOLD"...) sur /deals et /analyse suivent un pattern UI différent (en-têtes courts 2 mots), pas des chaînes de delta au sens de la tâche ; `AHA_DEALS`/`hc-live-card` dans `HomeCinematic.js` et `FLOAT_CARDS` dans `WowFx.js` étaient déjà conformes (les deux premiers sont en plus du code mort jamais rendu, confirmé en cherchant `<AhaMomentSection`/`<LiveSection` dans le JSX final — zéro appel). Vérifié en direct après build propre (redémarrage du serveur preview pour éviter le piège `.next` déjà documenté) : "vs cote" présent dans le DOM de la home, "vs marché" absent ; `/api/dashboard/summary` (401) et `/api/deals` (200) tous deux sains, zéro erreur console.

- [x] **2.5 — Fraîcheur des prix.** 2026-07-03 — fait, avec deux clarifications importantes trouvées en creusant. **Payloads corrigés** (même bug dans les deux : `fetchedAt` déjà tracké en interne mais jamais inclus dans l'objet retourné) : `buildAuctionDealsPayload()` (`auctionDeals.js`, les 4 chemins de retour) et `getTopOpportunites()` (`opportunitesTop.js`, consommé par `/api/trending`, les 4 chemins de retour) — `hottest` avait déjà `fetchedAt` depuis la tâche 1.2, rien à faire là. **Découverte 1** : `formatAgo` (HomeCinematic.js) n'existe QUE dans `LiveSection`, confirmé mort (jamais rendu). Le déplacer aurait dupliqué `formatRelativeTime` qui existe déjà dans `lib/timeFormat.js` et sert déjà ce rôle partout ailleurs sur le site (utilisé cette session dans PriceProvenance, dashboard, etc.) — non déplacé, discipline anti-duplication plutôt que suivre la lettre d'une tâche écrite avant que je découvre ce doublon. **Découverte 2** : le carrousel trending consommateur de `/api/trending` (`HomeTrendingSection.js`/`HomePremium.js`) est lui-même **mort** (non importé par `page.js`) — même mal endémique que `LiveSection`. Le fix backend (`fetchedAt` exposé) reste fait et utile pour une future réactivation, mais il n'y a pas d'UI vivante à laquelle attacher une étiquette de fraîcheur pour l'instant. **UI corrigée sur du code vivant** : `RefreshBar.js` (consommé par `/deals` Hottest Deals et `/portfolio`) — nouvelle classe `.rb-bar--stale` (ton warning ambre) si > 72h ; `/encheres` — `formatFreshness()` affiche heure seule si < 24h, date + heure si plus vieux (l'ancien affichage "14:32" sans date était ambigu passé 24h), + classe stale > 72h. Vérifié en direct après build propre (redémarrage préalable du serveur preview) : `/api/auctions/ending-soon` et `/api/trending` exposent maintenant `fetchedAt` (confirmé par fetch réel, 10 joueurs pour trending), `/encheres` affiche "Mis à jour 08 h 50" correctement calculé depuis les vraies données, zéro nouvelle erreur console (même bug de clés dupliquées pré-existant, déjà documenté, inchangé).

---

## PHASE 3 — LOGISTIQUE DU SCORE (le cœur — le rendre irréprochable)

**État actuel** : score v7.1, 13 facteurs pondérés (Performance 14%, Momentum 10%, Accélération 8%, Âge 10%, Marché 10%, Liquidité 4%, Upside 14%, Hype 7%, Discrépance 5%, Risque 5%, Catalyseurs 6%, Social 3%, Équipe 4%). Les 4 sous-scores avancés tournent via cron `enrich-scores` quotidien — un score frais de la journée peut être "de base" (sans les 4 avancés) sans que l'utilisateur le sache. L'explicabilité (narratif + chat "convaincs-moi" + `/api/score/explain`) existe mais est peu mise en avant. Le backtest public existe (`/backtest`) mais est orphelin.

- [x] **3.1 — Poids du score : une seule source de vérité.** 2026-07-04 — fait. Créé `SCORE_WEIGHTS`/`SCORE_WEIGHTS_BY_KEY`/`weightToPct()` dans `lib/cardScoutScoreMath.js` (fichier sans `"server-only"`, donc importable côté client ET serveur — contrainte clé, `cardScoutScore.js` a `"server-only"` et ne peut pas être importé depuis un composant client). `lib/cardScoutScore.js` : `FACTOR_WEIGHTS` dérive maintenant de `SCORE_WEIGHTS_BY_KEY` (zéro changement de valeur, juste dé-dupliqué). **Audit complet effectué** (`/player`, `/opportunites`, home, ScrollStory, HomeFeatureTabs) avec une découverte importante : sur `/player`, il existe **4 copies indépendantes** du même array de facteurs pondérés (aucune ne s'importait d'une autre) — `app/player/[id]/PlayerAiScoreClient.js` et `app/player/[id]/ScoreBreakdownModal.js` se sont avérées être du **code mort** (zéro importeur trouvé nulle part dans le repo, vérifié par grep exhaustif) ; le vrai composant rendu par `/player` est `components/ui/spatial-player-showcase.jsx` (**extension `.jsx`**, ratée par mes premiers grep limités à `*.js` — piège à noter). Ce fichier avait un vrai bug utilisateur : `hype` à 11% au lieu de 7%, `teamContext` à 0% au lieu de 4% (texte "poids à 0% pendant la validation" devenu faux depuis l'activation de teamContext le 2026-06-21). Corrigé pour dériver de `SCORE_WEIGHTS_BY_KEY`, même pattern (garde localement label/icône/texte explicatif, weight vient de la source unique). `app/home/ScrollStory.js` (composant réellement rendu par la home, confirmé via l'export JSX de `HomeCinematic()`) : poids déjà corrects mais dupliqués en dur — remplacés par des lookups. **`app/home/HomeCinematic.js`** (`ScoreMock`/`SCORE_FACTORS`, `FeaturesSection` avec le texte "Hype (14%)", `ScoreSection`) et **`app/home/ScoreCircuit.js`** : confirmés comme code mort (aucun ne fait partie de l'arbre JSX réellement exporté par `HomeCinematic()` — `HomePremium.js` qui importe aussi `ScoreCircuit` est lui-même orphelin) — **non touchés ici**, déjà couverts explicitement par la tâche 5.4 (élagage `HomeCinematic.js`), corrigé les poids là-bas aurait été un travail jeté. `/opportunites` (`OpportunitesClient.js`) : aucun affichage de poids trouvé, rien à corriger. Vérifié : lint zéro nouvelle erreur ; `POST /api/score` en direct confirme les facteurs avec les bons poids serveur (`hype: weight 0.07`, etc. — le calcul mathématique était déjà correct, seul l'affichage UI était faux). **Limite de vérification** : impossible de confirmer visuellement le rendu final des pourcentages sur `/player` (capture d'écran) — un bug séparé et non lié bloque le hero en skeleton indéfiniment (voir section Bugs découverts). Le calcul serveur et le code du composant ont été vérifiés directement (JSON `/api/score` + lecture du diff), confiance élevée malgré l'absence de capture visuelle.

- [x] **3.2 — Badge de complétude du score.** 2026-07-04 — fait. `lib/cardScoutScore.js` : nouveau `deriveEnrichmentStatus(factors)` — vrai seulement si les 4 clés `*Meta` (`marketDiscrepancyMeta`, `teamContextMeta`, `catalystsMeta`, `socialAttentionMeta`) sont présentes, ce qui distingue fiablement un score fraîchement recalculé par le cron hebdo (`fastMode`, jamais ces 4 clés) d'un score déjà passé par le cron quotidien `enrich-scores`. `enriched`/`computedAt` ajoutés à **tous** les points de retour de `scoreCardScoutWithClaude` (fastMode, cache-hit, échec, succès final) + à la branche "lecture DB directe" de `app/api/score/route.js` (qui ne renvoyait avant que `stored.data` brut, perdant `computedAt` en route). **Bug réel trouvé et corrigé en cours de route** : la branche finale (après ajustement DeepSeek) reconstruisait `factors` via `buildFactorsOutput(scores, weightedScore, scoreAdjustment)` au lieu de réutiliser `baseFactors` — qui portait déjà les 4 `*Meta` calculés quelques lignes plus haut — donc un score fraîchement calculé en mode complet (hors cache, hors fastMode) perdait silencieusement ses sous-scores avancés dans la réponse finale. Corrigé en dérivant `factors` de `baseFactors` (mêmes `scores`/`weightedScore`, ajoute juste `scoreAdjustment`) — comportement préservé, juste les champs `*Meta` ne sont plus jetés. UI : nouveau `ScoreCompletenessBadge` dans `components/ui/spatial-player-showcase.jsx` (le composant réellement rendu par `/player`, confirmé tâche 3.1), placé juste sous le `ViewToggle` — visible dans les deux vues (Orbital/Détaillé) sans dupliquer le code. Vert "Score complet · maj il y a Xh" (tooltip listant les 4 facteurs) si `enriched`, neutre "Score de base · enrichissement cette nuit" sinon. `formatRelativeTime` (`lib/timeFormat.js`, déjà client-safe) pour l'écart de temps. CSS ajoutée dans `spatial-showcase.css` (`.sp-completeness`/`--full`/`--base`, tons `--profit`/`--ghost` cohérents avec les badges de provenance de prix de la Phase 2). Vérifié en direct : `POST /api/score` pour Auston Matthews retourne `enriched:true`, `computedAt` réel, les 4 `*Meta` présents ; capture d'écran confirmant le badge vert "Score complet · maj il y a 23h" sur les deux vues, desktop et mobile 375px (aucun scroll horizontal). Build + lint zéro nouvelle erreur.

- [x] **3.3 — Deltas de score partout.** 2026-07-04 — fait, avec un périmètre ajusté après audit de chaque page candidate (honnêteté > suivre la lettre de la description d'origine). Backend : `getScoreDeltas(playerIds)` extrait dans `lib/playerScores.js` (batché, dédupliqué), `/api/dashboard/summary/route.js` refactoré pour l'utiliser (zéro changement de comportement, ~45 lignes de logique dupliquée supprimées). **Audit des 5 cibles listées** : (1) **`/opportunites`** — fait : `app/api/opportunites/top/route.js` enrichit chaque opportunité d'un `scoreDelta` calculé À CHAQUE requête (jamais mis dans le cache 14j des picks, sinon le delta figerait 2 semaines) ; `OpportunitesClient.js` affiche `<ScoreDelta>` sous la jauge (`OppRankedItem`, le seul composant réellement rendu — `LeagueOpportunityCard`/`LeagueOpportunitiesLayout` confirmés code mort, jamais invoqués, non touchés). (2) **`/pulse`** — fait : `app/api/pulse/route.js` calcule les deltas après lecture du cache mémoire 1h (jamais mis en cache non plus, même raison), affiché sur `PlayerCard` (grille) et le modal détail. (3) **`/deals` — délibérément exclu** : le score affiché par carte (`d.investmentScore`) est le score de l'ANNONCE (DeepSeek, `dealInvestmentScore.js`), pas le Card Metrics Score du JOUEUR (13 facteurs) — accrocher un delta de score joueur à côté d'un score d'annonce aurait mélangé deux métriques différentes et induit en erreur, contraire à l'esprit "honnêteté = crédibilité" de cette phase ; aucun score joueur n'est d'ailleurs affiché par carte sur cette page (juste un lien nom-joueur). (4) **Carrousel trending — délibérément exclu** : confirmé mort dès la tâche 2.5 (composant jamais importé par `page.js`), rien à instrumenter. (5) **Page player — déjà fait**, confirmé : `ScoreChangeExplainer.js` affiche déjà un delta 14j (narratif + sparkline), plus riche que le chip générique — pas dupliqué. Composant `app/components/ScoreDelta.js` (+ `score-delta.css`) : flèche SVG up/down, tons vert/rouge/gris cohérents avec `.dash-watch__delta` existant, "Nouveau" si `!hasHistory`. Vérifié en direct : `/api/opportunites/top` et `/api/pulse` retournent des `scoreDelta` réels et cohérents (ex. Macklin Celebrini +0.1/7j) ; capture d'écran confirmant l'affichage sur les deux pages (grille + modal pour /pulse, jauge pour /opportunites), desktop et mobile 375px, aucun scroll horizontal. Build + lint zéro nouvelle erreur.

- [x] **3.4 — Explicabilité mise en avant sur /player.** 2026-07-04 — fait. Nouveau `ExplainTabs` (3 onglets : Facteurs / Pourquoi ça bouge / Convaincs-moi) dans `components/ui/spatial-player-showcase.jsx` (le composant réellement rendu par `/player`, confirmé tâche 3.1) — même famille visuelle que le `ViewToggle` Orbital/Détaillé existant mais un niveau au-dessus, en pill-bar. **Facteurs** = contenu existant inchangé (le toggle Orbital/Détaillé + les 13 barres/poids devient un sous-contrôle imbriqué dans cet onglet — aucune régression, juste redécoré). **Pourquoi ça bouge** = `ScoreChangeExplainer` (delta 14j + narratif + mini-sparkline, déjà existant) **déplacé** ici depuis un rendu permanent en dessous du hero vers un montage conditionnel (`{activeTab === "pourquoi" && <ScoreChangeExplainer .../>}`) — le composant lui-même n'a pas changé, seul l'endroit/moment de son montage a changé, ce qui suffit à le rendre lazy (son fetch `/api/score/explain` est dans un `useEffect` au montage, donc ne se déclenche plus tant que l'onglet n'est pas ouvert). **Convaincs-moi** = `ScoreChatDrawer` simplifié : suppression complète du chrome overlay/drawer (bouton déclencheur, backdrop, panneau coulissant, bouton fermer, verrouillage du scroll, `Escape` pour fermer) — désormais un panneau inline directement dans l'onglet, plus simple et plus cohérent avec les 2 autres onglets qu'un drawer qui s'ouvrait par-dessus. Déjà lazy par nature (aucun appel avant l'envoi d'un message) ; `autoFocus` retiré de l'input (pertinent en modal, pas en panneau d'onglet — évite un saut de scroll à chaque changement d'onglet). `app/player/[id]/page.js` : suppression des rendus permanents `<ScoreChangeExplainer>` et `<PlayerChatSection>` (fonction + Suspense, devenue morte) — ces deux composants sont maintenant importés et pilotés depuis `spatial-player-showcase.jsx`. Accessibilité : labels de l'onglet masqués sous 480px (icônes seules pour économiser l'espace) mais `aria-label` toujours présent sur chaque bouton pour ne pas casser le nom accessible. Vérifié en direct sur Auston Matthews : les 3 onglets rendent correctement (capture d'écran desktop + mobile 375px), le fetch `/api/score/explain` confirmé absent tant que l'onglet "Pourquoi ça bouge" n'est pas cliqué (vérifié via le réseau), le chat testé de bout en bout (question suggérée → vraie réponse DeepSeek affichée). Build + lint zéro nouvelle erreur (mêmes warnings pré-existants `opportunitesTop.js`/`trendingData.js`, non liés).

- [x] **3.5 — Backtest comme preuve sociale.** 2026-07-04 — fait. Vérifié d'abord `/api/backtest` en direct : couverture actuelle **0 joueur** (pire que les "~6" du backlog — la collecte d'historique n'a que 15 jours), mais la page gère déjà ça honnêtement ("Collecte de données en cours… Revenir dans ~15/75 jours" selon la fenêtre), donc rien à corriger côté disclaimer — la page "tient debout". 3 liens ajoutés : (a) `components/ui/spatial-player-showcase.jsx` — nouveau `.sp-backtest-link` juste sous `ExplainTabs`, visible sur les 3 onglets (c'est une question de confiance globale dans le score, pas spécifique à un onglet) ; (b) `app/home/HomeCinematic.js` — carte "Moteur IA" de `TransparenceSection` (`TRANSPARENCY_ITEMS`, confirmée live) gagne un champ `linkHref`/`linkLabel` optionnel, rendu conditionnellement ; (c) `app/components/Footer.js` — nouvelle entrée "Backtest du score" dans la colonne Explorer. Vérifié en direct : lien home + footer confirmés (href `/backtest`, texte correct). **Limite de vérification** : le lien sur `/player` n'a pas pu être confirmé visuellement dans cette session — le bug de hang NHL déjà documenté (fetch sans timeout dans `lib/nhlPlayerLanding.js`, section "Bugs découverts") a bloqué le hero en skeleton pendant toute la fenêtre de test, y compris après redémarrage du serveur preview. Changement à très faible risque (3 lignes JSX, import `Link` standard, classe CSS ajoutée) — confiance élevée via lecture de code malgré l'absence de capture d'écran. Build + lint zéro nouvelle erreur.

- [x] **3.6 — Score des annonces : montrer le raisonnement.** 2026-07-04 — déjà fait, aucun changement de code nécessaire. Vérifié en lisant `lib/dealInvestmentScore.js` : le prompt DeepSeek génère déjà `reason` (1 phrase, règles strictes anti-vague — interdit "attractif"/"potentiel"/"intéressant", doit citer ≥2 éléments concrets), `holdTimeline`, `upside`, `verdict`, au même appel (zéro coût additionnel). Vérifié dans `app/deals/DealFinderClient.js` que ces champs sont déjà rendus : `dl-card__reason` affiché en permanence sur chaque carte (plus visible qu'un simple hover), `dl-card__meta` affiche HOLD + UPSIDE, et un `ScoreDetailModal` complet (ouvert au tap sur le badge score) reprend le raisonnement sous "Analyse IA". Vérifié en direct avec de vraies données (`/api/deals?player=Cole+Caufield`) : `reason:"Outburst à 65% marché, Caufield 25 ans 51B 37A, bon rapport qualité-prix pour parallèle récent."`, confirmé aussi rendu dans le DOM (`.dl-card__reason`). Cette tâche décrivait un état du produit antérieur à sa propre rédaction — le travail avait déjà été fait ailleurs dans le développement du site.

---

## PHASE 4 — DESIGN ROLLOUT (étendre le niveau "home" au reste du site)

**Règle générale** : la home est cinématique (vitrine) ; les pages outils (/deals, /dashboard, /player...) doivent être **premium mais denses et rapides** — reveals subtils, hover soignés, hiérarchie typographique, états vides/chargement travaillés. Pas de section épinglée ni de marquee sur les pages outils.

- [x] **4.1 — Extraire les primitives WOW réutilisables.** 2026-07-04 — fait, avec une déviation délibérée sur `CountUp`. `app/components/wow/SplitWords.js` créé (code identique à l'original, mêmes classes `hw-split__mask`/`hw-split__word` pour zéro changement visuel — ces classes restent définies dans `home-wow.css`, donc ce composant a une dépendance CSS implicite tant qu'il n'est utilisé que sur la home ; documenté ici pour la prochaine tâche qui l'adopterait ailleurs). **`CountUp` PAS dupliqué dans `app/components/wow/`** : un `app/components/CountUp.js` existait déjà (utilisé par `ScoreGauge.js`), avec une API équivalente (`value`/`suffix`/`duration`/`className` vs viewport-trigger au lieu d'un délai fixe — sans conséquence ici car le hero est toujours dans le viewport au montage). Créer un second composant du même nom dans un autre dossier aurait recréé exactement le genre de duplication que cette tâche vise à éliminer — réutilisé l'existant à la place. `HomeCinematic.js` : les deux fonctions locales supprimées, imports mis à jour (`SplitWords` depuis `wow/`, `CountUp` renommé `SharedCountUp` à l'import pour éviter toute ambiguïté de nom dans ce fichier). Nouveau `app/components/wow/wow.css` : `.wow-rise` (générique, `animation-timeline: view()`, applicable par classe à n'importe quel élément — contrairement à la version scopée existante dans `home-wow.css` qui ne cible que des sélecteurs home fixes), `.wow-btn-shine`, `.wow-card-hover` (nouveau, pattern lift+glow ice déjà vu sur `.dash-card`/`.hc-transparence-card`, généralisé). Importé globalement via `app/globals.css` — disponible sur tout le site pour les tâches 4.2+. Les classes existantes de `home-wow.css` (`.hw-btn-shine`, le bloc `@supports` scopé) laissées intactes plutôt que supprimées : le risque de régression visuelle sur la home dépassait le bénéfice d'un nettoyage immédiat — à réévaluer lors de la tâche 5.4 (nettoyage HomeCinematic.js) qui touche déjà ce fichier. Piège rencontré : après l'édit, `.next` a affiché une fausse erreur "Identifier 'SplitWords' has already been declared" (cache HMR obsolète, pas un vrai conflit — confirmé par grep, une seule déclaration dans la source) — résolu par redémarrage du serveur preview, comme documenté. Vérifié en direct après redémarrage : home affiche "Lesprosscorent."/"Lesautresespèrent." correctement animés (SplitWords), `<span class="cn-countup hw-trust__num">0+</span>` structurellement correct (le "0" est le gel d'animation connu de l'environnement de preview automatisé, `document.hidden`, pas un bug). Build + lint zéro nouvelle erreur.

- [x] **4.2 — Design pass /deals.** 2026-07-04 — fait, périmètre resserré sur les points à plus fort impact/risque le plus bas. Skill design interrogé (`--design-system`) : suggestion de palette "Vibrant & Block-based" violette écartée délibérément — casserait la cohérence avec le langage visuel déjà établi (dark/ice/gold) sur tout le reste du site ; gardé les tokens existants comme l'exige la charte du plan. **Fait** : (1) `DealSkeletonCard` — remplace le bloc gris générique `.dl-skel` par une structure fidèle à `DealCard` (média 4:3 + pastille score ronde + lignes titre/meta/prix), classes `.dl-skel-card*` avec le même shimmer ; (2) en-tête de résultats (`.dl-strip`) enrichi avec `CountUp` (composant partagé, tâche 4.1) sur le nombre d'annonces + nouveau sous-compte "X deals prioritaires" (verdict "Acheter", `useMemo` dérivé de `displayedListings`, ton `--profit`). **Délibérément non fait** : hover `.wow-card-hover` sur les cartes — `DealCard` est déjà enveloppée dans `TiltCard` (tilt 3D + zoom image au survol, plus riche), ajouter le lift générique par-dessus aurait été redondant voire en conflit avec le transform piloté par JS de TiltCard ; en-têtes de groupe sticky — les groupes actuels (`groupDisplayName`) sont des labels par carte, pas des sections multi-cartes groupées, restructurer la grille pour ça est un changement d'architecture plus large que ce que "design pass" impliquait, à reconsidérer séparément si Carlo le souhaite ; suggestions de joueurs trending sur l'état vide — nécessite de brancher le cache trending, repoussé faute de temps dans cette itération ; transition stagger — `Reveal` (déjà utilisé par carte via `index={i}`) fournit déjà un décalage d'apparition, jugé suffisant. Vérifié : lint zéro nouvelle erreur ; en direct sur `/deals?player=Cole+Caufield`, skeleton structuré confirmé (9 `.dl-skel-card` pendant le chargement Hottest Deals, indépendant du chargement joueur), compteur confirmé dérivé des bonnes données (2 `.dl-card` réels dans la même section que le `.dl-strip`, le "0" affiché est le gel d'animation connu `document.hidden` de l'environnement preview, pas un bug) ; mode compare togglé sans erreur console ; mobile 375px sans scroll horizontal (`scrollWidth === clientWidth`).

- [x] **4.3 — Design pass /player/[id].** 2026-07-04 — fait, essentiellement déjà couvert par les tâches 3.1-3.4 réalisées plus tôt dans cette même session : badges delta (3.3) et complétude (3.2) déjà en place sur le hero ; mini-graphe historique du score déjà présent (sparkline 14j dans `ScoreChangeExplainer`, maintenant dans l'onglet "Pourquoi ça bouge" de 3.4) — pas de nouveau graphe SVG séparé nécessaire, ça aurait dupliqué l'existant ; deals du joueur en bas avec provenances prix déjà faits (tâche 2.2, `PriceProvenance`). Vérifié `/api/og/player/[id]` en direct : 200 OK, `image/png`, design cohérent avec la marque actuelle (fond dark, glow ice, score en accent gold, vraies données joueur/score/tier). **Non fait, jugé à faible valeur ici** : `SplitWords` sur le nom du joueur et `CountUp` sur le score — le hero utilise déjà une visualisation riche (orbital 3D / jauge détaillée, tâche 3.1), ajouter ces animations aurait été redondant plutôt qu'un vrai gain. **Petite incohérence notée sans corriger** : le texte "Metrics" de l'OG image utilise `#c8102e` (rouge), alors que l'accent de marque ailleurs sur le site est `--ice` (#00d4ff cyan) — cosmétique, à revoir si quelqu'un retouche cette route pour une autre raison.

- [x] **4.4 — Design pass /opportunites.** 2026-07-04 — fait. La page (`OppRankedItem`/`OppRankedList`, le composant réellement rendu — `LeagueOpportunityCard` confirmé mort tâche 3.1) avait déjà : badge "TOP PICK", verdict par carte, tags de recommandations de cartes, CTA "Quick Scan" (analyse inline sans quitter la page — couvre déjà "voir les deals du joueur") + "Fiche détaillée", et `Reveal index={i}` (stagger déjà en place). Ajouté : (1) rang `#N` en gros — `.opp-item__rank-num` passe de 1.1rem/gris à 2rem/`--cn-hero`/`--platinum` (confirmé 32px en direct) ; (2) thèse IA (`opp-item__narrative`) stylée en citation — bordure gauche ice + italique ; (3) compteur réel "Prochaine édition dans X jours" (même calcul 1er/15 que le countdown dashboard tâche 1.5, pure date math client) remplace le texte statique "Rafraîchi chaque semaine" dans le badge hero — confirmé en direct : "Prochaine édition dans 11 jours". **Non fait** : delta de rang vs édition précédente — aucun historique de rang n'est actuellement stocké nulle part (seul le score courant est en DB), l'ajouter demanderait un nouveau mécanisme de snapshot de classement, hors scope d'une passe design ; à considérer comme tâche dédiée si voulu. Vérifié : lint zéro nouvelle erreur, changements confirmés en direct via DOM (police, texte, styles calculés), zéro erreur console.

- [x] **4.5 — Design pass /encheres + /pulse.** 2026-07-04 — fait. **Enchères** : `formatTimeLeft` retourne maintenant un `tier` à 3 niveaux (`normal`/`gold`/`red`) au lieu d'un booléen `urgent` binaire — gold sous 1h, rouge (avec pulse) sous 10min ; classes `.enc-card--gold`/`.enc-card__timer--gold` ajoutées, `.enc-card--urgent`/`.enc-card__timer--urgent` renommées en `--red`. Tri par urgence déjà en place côté backend (`buildAuctionDealsFresh`, `urgencyA = (24 - hoursLeft) + dealPct * 0.5`), donc déjà "visible" dans l'ordre de rendu — rien à changer là. **Pulse** : (1) icônes emoji du feed NHL (`FEED_ICONS`) remplacées par des SVG lucide-react (`ArrowLeftRight`/`HeartPulse`/`FileSignature`/`Zap`/`Newspaper`) — violaient le garde-fou "pas d'emojis comme icônes" du plan, trouvé en creusant cette tâche ; (2) `PulseClient` ne fetchait `/api/pulse` qu'une fois au montage — ajouté un poll 60s (le cache serveur reste 1h, mais la page reflète maintenant la nouvelle donnée dès qu'elle existe sans reload manuel) ; (3) nouvel indicateur `.pulse-live-dot` (point vert pulsant, respecte `prefers-reduced-motion`) à côté de "Mis à jour". Timestamps relatifs et reveals étaient déjà en place sur les deux pages (non touchés). Vérifié en direct : `/pulse` confirme le dot live + 20 icônes SVG (zéro emoji) + poll actif (code revu, cache serveur empêchant un test de changement de données réel dans la fenêtre de session) ; `/encheres` confirme la logique de tier sans erreur (aucune enchère actuellement <1h pour voir le gold/rouge en trafic réel, mais le calcul est vérifié par lecture de code — comparaison numérique simple). Build + lint zéro nouvelle erreur.

- [x] **4.6 — Design pass /analyse.** 2026-07-04 — fait. `AnalyzeProgressSteps` — le backend (`/api/analyze-listing`) ne rapporte pas ses étapes en temps réel (une seule réponse finale), donc séquence calée sur une estimation de durée réelle (~4-6s) via `setTimeout`, clairement étiquetée comme étapes d'analyse ("Lecture de l'annonce…" → "Identification du joueur…" → "Calcul de la cote…" → "Verdict…"), jamais de fausse donnée affichée. Verdict en stamp : nouveau `.an-verdict__stamp` (remplace le petit badge `cn-badge` par un vrai stamp — bordure 3px, `--cn-hero`, glow), réutilise la variable CSS `--an-verdict-c` déjà définie par tone (`.an-verdict--buy/watch/avoid`) plutôt que de dupliquer `.hs-ph__stamp` qui est fixé au vert — ici la couleur suit le verdict réel (rouge pour "Passer", etc.), plus honnête que toujours vert. Grille de données avec provenance prix (`PriceProvenance`) déjà en place (tâche 2.2), non touchée. Vérifié en direct avec une vraie annonce eBay (Cole Caufield) : le stamp s'affiche avec la bonne couleur/taille (`rgb(239,68,68)` rouge pour verdict "Passer", `font-size: 41.6px` ≈ 2.6rem, `text-transform: uppercase`), zéro erreur console, mobile 375px sans scroll horizontal. La séquence de progression elle-même n'a pas pu être ré-observée en plein milieu (l'analyse s'est terminée avant la vérification suivante, ré-déclencher exactement le bon timing s'est avéré peu fiable en preview automatisée) — confiance élevée via lecture de code (timers simples, classes conditionnelles). Build + lint zéro nouvelle erreur.

- [x] **4.7 — Cohérence navigation + footer.** 2026-07-04 — fait (audit, sans changement de code). **Découverte notable** : le prop `active` passé à `<AppNav active="...">` sur les ~20 pages du site est en réalité **mort/inerte** — `AppNav.js` calcule son propre état actif en interne via `usePathname() === item.href` (lignes 133/200), le paramètre `active` est destructuré (`{ active = null }`) mais jamais lu ensuite. Conséquence pratique : l'état actif de la nav est déjà correct partout (piloté par l'URL réelle, donc auto-cohérent par construction, y compris pour `/dashboard` qui passe `active="/dashboard"` — un format différent des autres pages qui passent des clés nues comme `"deals"` — incohérence sans impact puisque la valeur n'est de toute façon jamais utilisée). Non corrigé ici (supprimer le prop sur ~20 fichiers pour un gain purement cosmétique/lisibilité, zéro changement de comportement utilisateur, mieux placé en Phase 5 élagage si voulu — noté ci-dessous). **Footer** : tous les liens (`Footer.js`, colonnes Explorer/Mon Compte + bas de page) vérifiés — chacun résout vers une vraie route `page.js` existante (aucun 404), le lien `/backtest` ajouté en tâche 3.5 déjà présent. Titres de page (`generateMetadata`) déjà cohérents sur les pages vérifiées cette session (`/player/[id]`, `/opportunites`, etc.). Pas de breadcrumb sur le site (convention actuelle : lien "← Accueil" en haut de chaque page outil) — cohérent partout, pas de changement nécessaire.

- [ ] **4.8 — Audit mobile global.** Passe systématique 375px sur : home, dashboard, deals, player, opportunites, encheres, pulse, analyse, portfolio, watchlist. Checklist par page : pas de scroll horizontal, touch targets ≥44px, textes ≥16px pour le body, bottom nav fonctionnelle, modales utilisables. Corriger ce qui est trouvé. Documenter dans la note de la tâche ce qui a été corrigé.

---

## PHASE 5 — ÉLAGAGE & CONSOLIDATION (moins mais mieux)

**Constat** : 23 pages, plusieurs se recoupent ou sont orphelines. Un site avec 6 features excellentes > 12 features moyennes. Pour chaque suppression : redirect 301 (`next.config` ou page avec `redirect()`), retirer du sitemap, retirer les liens entrants.

- [ ] **5.1 — Supprimer /grading.** Déjà retiré de la nav/footer/sitemap (PSA/TAG ont fermé les soumissions). La page existe encore → supprimer `app/grading/`, redirect 301 vers `/analyse`. Vérifier qu'aucun composant ne l'importe.

- [ ] **5.2 — Fusionner /compare dans /deals.** Deux entrées pour comparer des joueurs (page `/compare` + mode compare de `/deals`). Garder LE mode /deals (déjà intégré au flux de recherche), rediriger `/compare` → `/deals`, migrer ce que /compare fait de mieux (si quelque chose) dans le mode deals. Retirer `/api/compare/player` si plus consommé (vérifier d'abord au grep).

- [ ] **5.3 — Consolider les canaux de notifications.** Quatre surfaces se recoupent : `/alertes`, `/notifications`, `/digest`, `/picks` (+ préférences dans `/parametres`). Cible : **/notifications** = centre unique in-app (alertes prix déclenchées + notifs) ; **/picks** reste (c'est du contenu, pas un canal) ; **/digest** et **/alertes** → leurs réglages migrent dans `/parametres` (section "Emails & alertes"), pages redirigées. Faire un audit d'usage d'abord (grep des liens entrants) et ajuster la cible si une page est en fait déjà morte.

- [ ] **5.4 — Nettoyer HomeCinematic.js.** ~700 lignes de sections définies mais non rendues (LiveSection, AhaMomentSection, FeaturesSection, HowItWorksSection, VaultSection, ScoreSection, AnalyseSection, RecentPlayersStrip). **Décision** : supprimer le code mort, SAUF `LiveSection` (Ça bouge maintenant — enchère chaude + hottest deal + top mover) qui est bonne pour la mission → la réintégrer dans la home entre ScrollStory et le marquee, avec les garde-fous fraîcheur de 2.5. Supprimer aussi le CSS orphelin correspondant dans `home-cinematic.css` (prudence : vérifier au grep chaque classe avant suppression).

- [ ] **5.5 — Audit /search et /a-propos.** `/search` : vérifier si la recherche nav pointe dessus ou si c'est un doublon du flux /deals — si doublon, rediriger. `/a-propos` : la garder (crédibilité) mais vérifier qu'elle est à jour avec la mission, liée depuis le footer, et alignée sur le design system.

- [ ] **5.6 — Audit des crons et routes API mortes.** ~18 routes cron/API. Vérifier via `cron_runs` (table de monitoring existante) et `vercel.json` lesquelles tournent vraiment. Identifier les routes sans consommateur (grep). Rapport dans la note de tâche + suppression de ce qui est confirmé mort. Ne PAS toucher aux crons actifs (opportunites, enrich-scores, snapshot-scores, card-prices, hottest, trending, auctions, price-alerts, watchlist-alerts, weekly-picks, daily-digest, welcome-emails).

---

## PHASE 6 — NEXT LEVEL (une fois les fondations solides)

- [ ] **6.1 — PWA basique.** `manifest.json` (icônes, theme #05060a, standalone) + service worker minimal (cache statique, offline fallback page). La majorité des users est mobile ; c'est l'étape prévue post-auth dans la roadmap. Pas de push notifications dans cette tâche.

- [ ] **6.2 — Perf pass.** Lighthouse sur home, deals, player, dashboard. Cibles : LCP < 2.5s mobile. Points connus à vérifier : vidéo hero (poster + `preload="none"` + fallback statique mobile — les fichiers `/video/hero-bg.*` 404 actuellement, corriger ou retirer proprement), images NHL (lazy + sizes), payloads API (le summary dashboard doit rester < 50KB). Corriger le plus impactant, documenter le reste.

- [ ] **6.3 — Smoke tests.** Créer `scripts/smoke.mjs` : fetch de ~12 routes clés (pages SSR + APIs publiques) contre localhost:3001, vérifie status 200 + un marqueur de contenu par page (ex. "Card Metrics Score" sur /player/8481540). Exécutable via `npm run smoke`. C'est le filet de sécurité de la loop pour les itérations suivantes — l'ajouter au protocole de vérification (section 1) une fois créé.

- [ ] **6.4 — Onboarding "aha" plus rapide.** Le WelcomeTour existe. L'améliorer : après le choix langue/marché, proposer directement "choisis 3 joueurs à suivre" (liste des trending) → la watchlist n'est plus vide → le dashboard a du contenu dès la première session. Mesurer via `/api/track` (event `onboarding_players_followed`).

- [ ] **6.5 — Empty states intelligents partout.** Audit de tous les états vides (watchlist, portfolio, alertes, picks, notifications, deals sans résultat) : chaque état vide doit (a) expliquer la valeur de la feature en 1 phrase, (b) offrir UNE action concrète, (c) être beau (icône, ton du design system). Un user qui tombe sur du vide doit repartir avec un prochain pas.

- [ ] **6.6 — Partage social des scores.** Bouton "Partager" sur /player : copie un lien + l'OG image du joueur (route existante) rend le score visible dans le partage. Vérifier le rendu OG sur les dimensions Twitter/Discord. C'est le levier d'acquisition le moins cher — chaque score partagé est une pub.

---

## BUGS DÉCOUVERTS EN COURS DE ROUTE (hors scope de la tâche qui les a trouvés — à corriger séparément)

- **`lib/trendingData.js` importe `computeCardScoutScore` depuis `cardScoutScoreMath.js`, qui ne l'exporte plus** (renommé/refactoré à un moment donné). Provoque un warning webpack ("Attempted import error") dès que `trendingData.js` est touché, et ferait planter `buildPlayerRow`/`buildTrendingPayload` au premier appel réel. Découvert le 2026-07-02 pendant la tâche 1.3 (le nouvel import `dealsHottest.js` → `trendingData.js` a exercé ce chemin dans les logs dev) — **pas causé par cette tâche**, `TRENDING_PLAYER_IDS` (ce que `dealsHottest.js` utilise) est juste une constante top-level, jamais impactée. **Correction 2026-07-03 (tâche 2.5)** : vérifié que `/api/trending` (la route live consommée par le carrousel de la home) **n'appelle PAS `buildTrendingPayload`** — elle utilise `getTopOpportunites()` (`opportunitesTop.js`), un chemin totalement différent, donc le carrousel trending de la home n'est PAS à risque. Seul `/api/cron/trending` (route.js) appelle encore `buildTrendingPayload` → c'est CE cron qui planterait, pas la page publique. Non corrigé ici par discipline de scope. À corriger dans une tâche dédiée : trouver le nouveau nom de la fonction équivalente dans `cardScoutScoreMath.js` (ou `cardScoutScore.js`) et mettre à jour l'import, puis vérifier `/api/cron/trending` en vrai.

- ~~**`/encheres` (`EncheresClient.js:183`) : clés React dupliquées dans la liste d'enchères**~~ **Corrigé 2026-07-03** (hors loop, suite à un signalement direct de Carlo — "LE SITE A DES ISSUES"). Cause confirmée en direct : la même annonce eBay pouvait matcher la recherche de plusieurs joueurs différents (`buildAuctionDealsForPlayer` tourne indépendamment par joueur) et se retrouver deux fois dans `allDeals` avant le tri final. Fix dans `lib/auctionDeals.js` (`buildAuctionDealsFresh`) : déduplication par `itemId` juste avant le tri/slice. Vérifié en direct après rebuild du cache (`?refresh=1`) : 0 doublon dans la réponse API, DOM `/encheres` confirmé (6 cartes = 6 items uniques), 0 erreur console sur un serveur preview fraîchement redémarré. Commit `404116a`.

- **`lib/opportunitesTop.js` : le champ `lastUpdated` affiché sur `/opportunites` ("MIS À JOUR : ...") était halluciné par DeepSeek** au lieu de venir de l'heure serveur réelle — le prompt demandait juste `"lastUpdated": (date ISO)` sans fournir la date courante, et le modèle n'a pas de notion fiable du temps. Résultat en prod : badge figé à "8 avril 2025" alors que le cache Supabase (`cache_opportunites`, TTL 14j) était en fait dans sa fenêtre de fraîcheur — trompeur dans les deux sens (fausse impression de donnée périmée, ou l'inverse). Découvert et corrigé 2026-07-03 (hors loop, même signalement Carlo). Fix : `mergeNarrativeWithScoredTop8` utilise systématiquement `new Date().toISOString()`, ne fait plus confiance à `claudeRaw.lastUpdated`. Vérifié en direct après `?refresh=1` : badge affiche maintenant "3 juillet 2026 à 00 h 34" (heure réelle de génération). Commit `d15a9ff`.

- **`lib/nhlPlayerLanding.js` (`fetchPlayerLanding`, ~ligne 24) : le `fetch()` vers l'API NHL (`api-web.nhle.com/.../landing`) n'a aucun timeout.** Conséquence observée en direct sur `/player/[id]` (tâche 3.1, 2026-07-04) : le hero (photo, nom, score, facteurs — `PlayerHeroSection` → `getPlayerLandingCached` → `fetchPlayerLanding`) reste bloqué en skeleton (`pl-hero--skeleton`) **indéfiniment** (testé >30s d'attente, jamais résolu) dès que cet appel externe traîne — `getPlayerLandingCached` utilise React `cache()`, donc toutes les sections de la page qui en dépendent (stats, prix) restent bloquées avec lui. Non corrigé ici : pas causé par les changements de cette tâche (aucun fichier touché ne fait partie de cette chaîne), et le rayon d'impact (ajout d'un timeout + fallback) mérite sa propre tâche plutôt qu'un fix improvisé en fin d'itération. Probable environnement de sandbox pour cette session (l'egress réseau semblait capricieux — `curl`/`Invoke-WebRequest` local ont aussi échoué à joindre le serveur dev depuis Bash/PowerShell), mais le manque de timeout reste un vrai risque de résilience en prod si l'API NHL est lente. **À corriger** : `AbortSignal.timeout(Xs)` sur ce `fetch` (et son jumeau `game-log` juste en dessous) + fallback honnête (skeleton avec message d'erreur après timeout, pas un hang silencieux).

---

## ANNEXE — Carte du code (référence rapide pour la loop)

**Pages** : `/` (home cinématique), `/dashboard` (+DashboardClient), `/deals` (Deal Finder + compare), `/opportunites` (top-10), `/player/[id]`, `/analyse` (URL eBay → verdict), `/encheres`, `/pulse`, `/portfolio` (vault), `/watchlist`, `/picks`, `/notifications`, `/alertes`, `/digest`, `/parametres`, `/search`, `/compare`, `/grading` (à supprimer), `/backtest`, `/a-propos`, `/auth/login`, `/admin` (mission control, séparé).

**Lib clés** : `dealFinder.js` (orchestrateur eBay→score), `ebayServer.js` (OAuth + fetch eBay, ×1.37 hardcodé), `soldPrices.js` (130point sold comps), `dealInvestmentScore.js` (DeepSeek scoring annonces), `cardScoutScore.js`/`cardScoutScoreMath.js` (score joueur 13 facteurs), `opportunitesTop.js` (top-10 batch), `playerScores.js`, `scoreEnrichment.js` (4 sous-scores avancés), `portfolioValue.js`, `auctionDeals.js`, `persistentCache.js` (cache_generic Supabase), `cronLog.js` (cron_runs), `ebayAffiliate.js`, `sportConfig.js` (sport-agnostic).

**Supabase** : `cache_generic` (cache persistant), `player_scores`, `player_scores_history` (snapshots), `watchlist`, `portfolio_cards`, `price_alerts`, `notifications`, `cron_runs`.

**Env dev** : port 3001 (Docker occupe 3000), `npm run dev -- --port 3001` via launch config `card-scout`. Lint : `npm run lint`. Pas de tests (jusqu'à 6.3).

---

*Fin du plan. Prochaine itération : première case `[ ]` en partant du haut.*
