import "../cinematic.css";
import "./picks.css";
import PicksClient from "./PicksClient";

export const metadata = { title: "Picks Hebdo | Card Scout" };

export default function PicksPage() {
  return <PicksClient />;
}
